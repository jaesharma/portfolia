import React from "react";
import { StyledProfileIcon } from "../styles/components/buttonStyles";

const ProfileIcon = ({ icon }) => {
	return <StyledProfileIcon src={icon} />;
};

export default ProfileIcon;
